import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 10,
    },
    serviceText: {
        fontSize: 16,
        fontWeight: "500",
        marginBottom: 7,
    },
    servicename: {
        margin: 10,
        backgroundColor: 'white',
        padding: 20,
        borderRadius: 7,
    }
})